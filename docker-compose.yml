services:
  subscrub:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    volumes:
      - h2-data:/app/data

volumes:
  h2-data:
